type Query {
   teamCalendar(teamId: ID!, language: Lang): [MatchDetail]
   clubTeams(clubId: Int, language: Lang): [Team]
}

type MatchDetail {
  id: String
  startDate: String
  homeTeam: Team
  awayTeam: Team
}

type Team {
  name: String
  id: String
}

enum Lang {
  nl
}
