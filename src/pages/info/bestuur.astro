---
import { getEntries } from '../../data/contentful/get-entries'
import { mapBestuursLid } from '../../data/contentful/mapping'
import Layout from '../../layouts/Layout.astro'
import { InfoBestuurPage } from '../../react/pages'

const leden = await getEntries('bestuurslid', mapBestuursLid)
---

<Layout title="Info - Bestuur" currentURL={Astro.url}>
  <InfoBestuurPage leden={leden} />
</Layout>
