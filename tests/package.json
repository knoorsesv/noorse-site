{
  "name": "tests",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "main": "homepage.spec.js",
  "scripts": {
    "test": "playwright test",
    "test:func": "playwright test --project=Functional",
    "test:local": "PLAYWRIGHT_TEST_BASE_URL=http://localhost:8000 pnpm test:func",
    "test:update": "playwright test --update-snapshots screenshots",
    "test:visual": "playwright test screenshots",
    "test:visual:local": "PLAYWRIGHT_TEST_BASE_URL=http://localhost:9000 playwright test screenshots"
  },
  "dependencies": {
    "@playwright/test": "1.17.1",
    "dotenv": "10.0.0"
  }
}
