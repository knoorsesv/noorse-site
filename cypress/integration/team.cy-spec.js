it('should have links to other teams in same category', function () {})

it('should show results fetched from kbvb graphql site', () => {})

it('should show team info fetched from contentful', function () {})
